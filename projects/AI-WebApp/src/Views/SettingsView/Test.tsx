export const App = reaxper(() => {
	

	return <div onClick={() => mutate(s => s.count++)}>
		<span>count:{1}</span>
		
		<div>333</div>
	</div>
})

import {reaxper} from 'reaxes-react';
import React, {} from 'react';
