export const reaxel_IPC_Listeners = reaxel(() => {
	const { store , setState , mutate } = orzMobx({
		
	});
	const rtn = {
		
	};
	return () => {
		return rtn;
	}
})



import { Position } from '#project/src/types/index';
import { IpcMainHandle } from '#project/src/utils/main/useIPC';
import { ipcMain } from 'electron';
