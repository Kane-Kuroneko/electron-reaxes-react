await mainWindowLoaded;

export const reaxel_IPCInterfaces = reaxel( () => {
	
	const listeners = {
		
	}
	
	let ret = {
		
	};
	
	return () => {
		
		return ret;
	};
} );


import { app } from 'electron';
import { mainWindowLoaded } from '../../../Main/initialize-main-window';
