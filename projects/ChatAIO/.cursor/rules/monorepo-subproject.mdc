---
description: ChatAIO 是 monorepo 子工程；git 操作须在仓库根目录执行
alwaysApply: true
---

# Monorepo 子工程工作区

- 当前目录 `projects/ChatAIO` 是 **electron-reaxes-react** monorepo 的子工程，不是独立 git 仓库。
- 编码规范、IPC、架构规则以 monorepo 根文档为准；本目录下 `.claude/`、`.qoder/`、`CODING_STANDARD.md` 等为指向根目录的软链接。
- 入口文档：本工程 [`AGENTS.md`](../../AGENTS.md)（相对 `.cursor/rules/` 为 `../../AGENTS.md`）。
- **需要 git 提交 / 查看仓库状态时**：将工作目录切换到 monorepo 根（`projects/ChatAIO/../..`），再执行 git；禁止在子工程目录当作仓库根操作。
- 未经用户显式要求，不得 `git commit` / `push` / `amend`（见 `git-commit-policy.mdc`）。
- **软链 / 新机**：`.git/config` 不进版本库。新 clone 或新机器上，Windows Git 常把本地 `core.symlinks` 写成 `false`，软链会退化成普通文本文件。在 monorepo 根先跑 `yarn setup:git-symlinks`（已退化则加 `--restore`）。详见 `AGENTS.md`「软链接说明」。
